[package]
name = "marshal-rs"
version = "3.0.0"
authors = ["savannstm <savannstm@gmail.com>"]
edition = "2021"
rust-version = "1.73.0"
description = "Blazingly fast, no_std-capable Ruby-lang Marshal implementation in Rust."
readme = "README.md"
repository = "https://github.com/savannstm/marshal-rs"
documentation = "https://docs.rs/marshal-rs/"
license-file = "LICENSE.md"
keywords = ["marshal", "ruby", "serialize", "deserialize", "no_std"]

[workspace]
members = ["crates/marshal-rs-capi", "crates/marshal-rs-wasm"]

[features]
default = ["std", "serde"]
# The DOM (Arena), Loader, Dumper and Value cursor API. Pulls in `alloc`.
alloc = []
# std::io-backed I/O. Pulls in `alloc`.
std = ["alloc"]
# Streaming Serialize/Deserialize for the arena. Pulls in `alloc`.
serde = ["alloc", "dep:serde"]

[dependencies]
serde = { version = "1.0.229", optional = true, default-features = false, features = [
    "alloc",
] }
thiserror = { version = "2.0.20", default-features = false }

[dev-dependencies]
criterion = { version = "0.8.2", default-features = false }
serde_json = "1.0.151"
serde_norway = "0.9.42"

[[test]]
name = "roundtrip"
required-features = ["alloc"]

[[test]]
name = "serde"
required-features = ["serde"]

[[example]]
name = "basic"
required-features = ["alloc"]

[[example]]
name = "fixture_check"
required-features = ["alloc"]

[[example]]
name = "yaml"
required-features = ["serde"]

[[bench]]
name = "load_dump"
harness = false
required-features = ["alloc"]

[[bench]]
name = "marshal_c_compare"
harness = false
required-features = ["alloc"]
