[package]
name = "marshal-rs"
version = "0.1.0"
edition = "2021"

[features]
sonic = ["dep:sonic-rs"]
default = ["dep:serde_json"]

[dependencies]
cfg-if = "1.0.0"
encoding_rs = "0.8.34"
serde_json = { version = "1.0.121", optional = true }
sonic-rs = { version = "0.3.9", optional = true }
